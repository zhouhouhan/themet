# 麦田世界《麦田与丝柏》导出规格（给 Blender 侧）

用途：油画《麦田与丝柏》的"画中世界"——观众按 E 从画框走进这片麦田。
网页端加载器已经写好并跑通，**只等这一个文件**。以下是它对这个文件的硬性要求与建议。

投放位置（文件名必须一致）：

```
assets/models/worlds/wheat-world.glb
```

---

## 一、必须满足（否则进不去或进去就错）

1. **单个 `.glb`，贴图内嵌**。不要 `.gltf + .bin + 贴图文件夹` 的散装形式，也不要 `.fbx`/`.obj`。
   网页只发一个 HTTP 请求，散装会 404。

2. **Blender 导出用默认的 `+Y up`，`Apply Modifiers` 打勾，Scale 保持 1.0**。
   不要手工旋转 -90°、不要额外套一个缩放的空物体父级。
   单位：米（真实世界尺度）。具体多大由加载器统一缩放，见第 4 条。

3. **原点放在"这片麦田地面的中心"**，地面是平的、位于 `y = 0`。
   加载器只做三件事：水平居中、整体缩放、把包围盒底部抬到 `y ≈ 0`。
   所以：
   - **可站立的地面必须是整个模型的最低面**。如果田里某处有个坑、或者下面还留了一段无关的低矮杂物，整个世界会被垫高，人会像站在看不见的台子上。
   - **不能有漂浮在半空、远离主体的碎块**，那会把包围盒撑大，导致场地区域被算小。

4. **水平尺寸 = 麦田本身的范围，不要套一个巨大的天空球。**
   加载器把整个模型的最大水平尺寸归一化到 **70 米**。
   如果外面裹了一个半径 500m 的天穹，那么"500m 的天穹 → 70m"，整片麦田会缩成几米大。
   - 推荐做法：**只导出麦田 + 地面 + 景物，天空交给我们**（网页会把背景刷成天空色）。
     如果你希望天空由模型自带，天穹直径请控制在麦田范围的 **1.3 倍以内**。
   - 导出时请告诉我：**含不含天穹**，以及天空的十六进制色值（我会把背景调成一致，避免天地接缝）。

5. **不要导出灯光和相机**。光照由网页提供（半球光 + 暖色平行光）。烘焙进贴图的"死光"会在实时渲染里发灰。
   材质只用 **Principled BSDF**，不要用 Blender 专有节点；不透明为主。

6. **麦穗/草丛这类镂空贴图，用 Alpha Clip（Cutout），不要用 Alpha Blend。**
   混合透明在网页端会有排序错误和明显掉帧。

7. **保有贴图，不要退化成顶点色。**
   我们之前踩过一次坑：模型被减面后颜色只剩顶点色，细节全丢，只能重建。
   **质量优先，体积交给我**——我会用 gltf-transform 压缩（之前的皇冠 55.6MB → 2.77MB）。

---

## 二、最好有（能省我一轮返工）

- 根节点命名成 `WHEAT WORLD` 之类，方便我定位。
- 麦穗如果成千上万株，请用 **实例化（Instanced Mesh）或合并**，不要上万个小物体，否则 draw call 爆掉。
- 导出前记录一下：**包围盒尺寸（长×宽×高）、三角面数、贴图清单及分辨率**。
  这几项直接决定我要不要再压一轮。
- 体积先控制在 **80MB 以内**（压缩前），超出也没关系，但请告诉我一声。

---

## 三、验收方式

文件放到位后我这边跑一遍：启动即预加载（不再二次等待）→ 按 E 无缝进入 → 落地站稳、能走动、能看四周 → 按 E 干净退出、回到原来站的位置和视角。
届时我会在浏览器里实测并回报：是否穿地、天/地是否接缝、帧率是否掉。

---

## 四、一句话总结

> 一个内嵌贴图的 `.glb`：米制、+Y 向上、原点在麦田地面中心、地面平且在 y=0、没有灯光相机、没有巨型天穹、保留了贴图（不是顶点色）、麦穗用 Alpha Clip、物体尽量实例化合并。

有拿不准的地方（比如天穹要不要、尺度合不合适）先问，别等导出完才发现要重来。
