# 项目深度审查与接手记录 · 2026-09-15

## 结论
已优先修正圣马可广场的进入视角，并完成主入口、展品交互、媒体、世界进出、输入与移动端布局的代码审查及本地回归。2026-09-15 15:52 的回归结果为 14 项全部通过、无 pageerror。当前工作只在本地完成，没有部署或重新打包发布。

## 广场视角
原先 2.4 秒入场镜头结束后切换到低位第三人称，塔顶容易离开画面。现使用持续的远景构图，初始相机约 (0,10,78)，看向 (0,24,-31)，基本垂直 FOV 40 度并随窄屏调整。玩家小范围移动时镜头轻微平移，保留完整塔顶、两侧建筑与天空。物理移动范围仍限制在原广场安全区域。此选择强调进入油画的观景体验，角色在初始构图中可能不显眼。

截图：verify-shots/deep-review-2026-09-15/piazza-panorama.jpg
模型资源仍为 piazza-san-marco-v2，无需重新导出 Blender。

## 已修复
- 输入：解除 E 与控制器载具切换冲突；忽略交互键重复事件；聊天输入不再被 WASD/A 等快捷键吞掉；打开卡片与聊天退出指针锁；清除按键同时清除速度。
- 世界加载：共享在途加载 Promise，取消与快速重试不会重复加载或覆盖新请求状态；进出保存和恢复光照、色调映射、相机控制状态；世界使用 AgX；限制世界像素比。
- 移动：显式传递帧 delta；聊天时暂停移动；返回展馆清除残余速度。
- 音频：缓存加载 Promise，以请求编号取消过期播放，避免离开后异步音频重新响起；结束状态与介绍队列同步。
- 视频：按展品法线定向，修正前墙视频朝向；播放成功后才显示画面，失败保留画作；开始体验时停止其他展品媒体。
- AI 聊天：关闭面板取消请求，30 秒超时，限制历史长度；添加 proxyUrl 接入能力；避免已关闭回复继续朗读。没有调用真实 AI 服务验证。
- 页面：手机卡片与聊天框适配；入口键盘导航、焦点与隐藏页面 inert；修正“真实比例”和旧麦田等文案。

## 尚未解决，发布前必须处理
### P1：浏览器公开配置包含 API 密钥
js/ai-config.js 含实际 apiKey，walktest.html 会加载它。浏览器端密钥无法保密。本文不记录密钥值，也未擅自删除或轮换凭据。发布前应把凭据移到服务端代理，客户端仅配置 proxyUrl；若该密钥曾随页面发布，应由账户管理者轮换。新增 proxyUrl 支持不等于已部署代理，当前风险仍存在。未核实线上是否已泄露，也没有测试外部服务。

## 验证证据与限制
可复跑脚本 tools/review-regression.cjs；结果 verify-shots/deep-review-2026-09-15/results.json。
14 项覆盖：加载层与全部展品卡片、长按 E、聊天输入、在途音频取消、视频法线、世界加载取消重试、入场后与移动后塔顶完整、四向碰撞、返回状态、重复进出光照、皇冠与影片开关、手机面板边界、页面异常、入口键盘导航。
测试使用本地 Chromium，阻止非本地网络请求，并使用受控帧推进。它不是实际帧率或真实手机性能测量，也不证明 iOS 音频策略、真实 AI 服务、所有触屏操作与完整影片播放已验证。视频朝向通过法线检查，不能替代逐帧视觉验收。

## 后续维护建议
- 在真实手机上补测触控移动、音频解锁及横竖屏。
- Gallery GLB 约 53 MB，后续测量冷启动网络和显存，再决定资源压缩与分包；本轮未声称性能优化达标。
- walktest-entry.mjs 仍为大型单文件，后续按输入、媒体、世界与 UI 拆分；本轮未作大范围结构改写。
- 历史麦田保留在 reference/retired-wheat-world，不要恢复旧集成。

## 文件与构建
主源文件 walkapp/walktest-entry.mjs；浏览器实际入口 js/walktest-bundle.js，二者均已更新。index.html 为入口页。修改前备份在 reference/before-deep-review-2026-09-15。
使用 walkapp/node_modules/.bin/esbuild.cmd 将 walkapp/walktest-entry.mjs 以 --bundle --minify --format=esm 输出到 js/walktest-bundle.js。当前源文件语法检查和构建通过。
tools/apply-deep-review-fixes.py 是本次的一次性迁移记录，不要重复执行；之后还有手工修正，以现有源文件为准。

## 第二轮视觉优化与复审
- 新增 walkapp/visual-polish.mjs，分离本轮视觉细化逻辑。浏览器 bundle 已同步构建。
- 展柜采用独立低铁玻璃近似材质，降低浑浊感；加入细铜框、玻璃角接件、顶底收边、内嵌底座和锁件。使用透明反射近似，无昂贵折射渲染，不声称物理准确玻璃。
- 广场增加 18 个静态时代服装人物剪影，以 10 个共享部件的 InstancedMesh 实现，变换位置、朝向、身高和衣色。均在 z<=39，避开玩家 z>=44.6 的活动区。人物是参考原画氛围的简化造型，不是逐人复刻，也没有动画；不增加模型下载。人物额外约 10 个主渲染 draw calls，阴影仍静态缓存。
- 广场专属材质处理降低饱和度、加入细微颗粒、提亮偏灰蓝天空、提高建筑粗糙度。没有全屏模糊、没有额外后处理渲染 pass；展馆材质不受该滤镜影响。
- 检查了广场全景和展柜近景。最终 15 项回归全部通过，新增实例部件数量验证；实例位置通过源代码检查。没有新增外部网络请求、没有部署。原公开 API 密钥的发布阻塞仍待处理。
- 最新截图：verify-shots/deep-review-2026-09-15/piazza-panorama.jpg 和 vitrine-detail.jpg。近景用于材质检查，不是实际行走默认机位。
- 当前效果在 walktest.html 中集成。独立 v2/preview.html 和原 Blender/GLB 没有同步这些运行时增强；接手者应保留 visual-polish.mjs 的集成调用。
- 本轮修改前备份 reference/before-visual-polish。性能结论仅为实现成本分析，尚未测真实手机帧率。
